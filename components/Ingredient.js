// Ingredient Class
class Ingredient {
  constructor(type, state) {
    this.type = type; // e.g., 'tomato', 'lettuce', 'meat'
    this.state = state; // e.g., 'raw', 'chopped', 'cooked'
  }

  // Updates the state of the ingredient
  updateState(newState) {
    // TODO: Implement state update logic
  }

  // Draws the ingredient on the screen
  draw(x, y) {
    // TODO: Implement drawing logic
  }
}
