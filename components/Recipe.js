// Recipe Class
class Recipe {
  constructor(name, ingredients, steps) {
    this.name = name;
    this.ingredients = ingredients; // e.g., ['tomato', 'lettuce']
    this.steps = steps; // e.g., ['chop', 'assemble']
  }

  // Checks if a given dish matches the recipe requirements
  isMatch(dish) {
    // TODO: Implement matching logic
  }
}
